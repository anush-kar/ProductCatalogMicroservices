server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/RATING_DB
    username: postgres
    password: example
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    root: INFO
